# Cấu hình chi tiết được lấy từ Config Server (config-repository/order-service.yml
# + application.yml). File local chỉ giữ tên service + nguồn cấu hình.
spring:
  application:
    name: order-service   # PHẢI khớp tên file trong config-repository và route lb://ORDER-SERVICE ở Gateway
  config:
    import:
      # .env: nạp secret (DB_PASSWORD...) - KHÔNG commit
      - optional:file:.env[.properties]
      # Config Server: nạp cấu hình tập trung
      - optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}
