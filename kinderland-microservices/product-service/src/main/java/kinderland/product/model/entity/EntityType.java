package kinderland.product.model.entity;

/** Loại entity mà ảnh gắn vào (1 bảng images dùng chung cho nhiều loại — đa hình). */
public enum EntityType {
    PRODUCT,
    BLOG,
    RETURN_REQUEST,
    PRODUCT_BRAND,
    SKU
}
